# **🎮 项目名称：墨客（WordWar）（MVP）**
多人在线文字聊天室网页游戏

## **一、项目概述**

本项目是一个基于浏览器的网页游戏，采用 **Next.js（前端 + API 路由） + Node.js WebSocket 服务 + SQLite 数据库** 架构。

用户登录后进入同一聊天室，在聊天中可：

- 互动发言；
- 进行经验存点和升级；
- 自动积累金钱；
- 参与“金币雨”活动；
- 与怪物战斗；
- 发起切磋。

系统需支持 **约 100 个同时在线用户**。

## **二、技术栈**

| **层级** | **技术**                                        |
| -------- | ----------------------------------------------- |
| 前端     | Next.js + React + TypeScript + TailwindCSS   |
| 实时通信 | WebSocket (ws 或 socket.io)                     |
| 后端     | Node.js（Next.js Route Handlers + WS 独立服务） |
| 数据库   | SQLite（better-sqlite3 或 Prisma ORM）          |
| 状态管理 | Zustand（前端）+ 内存存储（后端权威状态）       |
| 部署     | 单机 Node 服务（1~2 核 VPS 即可）               |

## **三、系统模块说明**

### **模块 1️⃣ 登录 / 聊天室 + WebSocket 心跳**

#### **功能目标**

- 玩家注册、登录、进入统一聊天室。
- 显示在线用户列表。
- 实现公共频道聊天。
- WebSocket 保持心跳，掉线检测与重连。

#### **功能细节**

| **功能**   | **描述**                                                     |
| ---------- | ------------------------------------------------------------ |
| 注册/登录  | 账号密码注册；JWT 登录（保存在 localStorage）。              |
| 聊天室界面 | 左：个人信息面板；中：消息流；右：在线用户列表。             |
| 聊天范围   | MVP 仅公共频道（后期扩展帮派、私信）。                       |
| 消息显示   | 公共聊天、系统事件、战斗结果共用消息流；带前缀区分类型。     |
| 心跳机制   | 客户端每 30 秒发送 "ping"；服务器返回 "pong"。超过 90 秒未响应视为离线。 |

#### **WebSocket 事件**

| **事件**               | **方向** | **说明**                   |
| :--------------------- | -------- | -------------------------- |
| connect                | C→S      | 建立连接时发送用户 token。 |
| chat.message           | 双向     | 发送或广播聊天消息。       |
| ping / pong            | 双向     | 心跳机制。                 |
| user.join / user.leave | S→C      | 通知在线人数变化。         |

### **模块 2️⃣ 存点 / 升级 + 被动金钱定时结算**

#### **功能目标**

用户在线可手动点击“存点”获得经验值；自动积累金钱；达到经验上限后升级并增加属性。

#### **游戏数值规则**

| **项目**       | **说明**                        |
| -------------- | ------------------------------- |
| 每分钟经验存点 | 12 点（需手动点击，1分钟一次）  |
| 升级经验       | 12 + 2*(等级-1)                 |
| 被动金钱/分钟  | 55 + 18*(等级-1)                |
| 属性成长       | 每级 +4 武力 / +18 HP / +2 防御 |
| 初始属性       | 武力 12 / HP 80 / 防御 4        |

#### **功能实现**

- 定时器每分钟计算一次金钱增长；
- 存点按钮带冷却 60 秒；
- 达经验上限后自动升级；
- 升级广播系统提示（如“玩家A升至5级！”）。

#### **API 接口**

| **方法**                | **路径**           | **功能** |
| ----------------------- | ------------------ | -------- |
| POST /api/auth/login    | 登录               |          |
| POST /api/exp/store     | 手动存点           |          |
| GET /api/player         | 获取角色状态       |          |
| WS event: player.update | 推送属性与金钱变化 |          |

### **模块 3️⃣ 金币雨（含每人 5 枚上限）**

#### **功能目标**

在公共聊天窗口中，后台触发“金币雨”活动，掉落金币图标，玩家点击抢金币。

#### **参数设定**

| **项目**     | **值**             |
| ------------ | ------------------ |
| 持续时间     | 20 秒              |
| 金币数量     | 25 枚              |
| 每枚金币价值 | 500 金币           |
| 每人上限     | 5 枚               |
| 触发方式     | 后台手动或定时触发 |

#### **行为流程**

1. 服务器发出 coinrain.spawn 事件；
2. 前端生成金币动画；
3. 玩家点击金币 → 发送 coinrain.hit；
4. 服务端判定命中与上限；
5. 成功则增加金币并推送 coinrain.result；
6. 20 秒后发送 coinrain.end 结束事件。

#### **WebSocket 事件**

| **事件**        | **含义**                                  |
| --------------- | ----------------------------------------- |
| coinrain.spawn  | 开始金币雨（包含 event_id、金币坐标数组） |
| coinrain.hit    | 玩家点击金币                              |
| coinrain.result | 返回是否成功拾取、当前已拾数量            |
| coinrain.end    | 金币雨结束、结果汇总                      |

#### **服务端逻辑（伪代码）**

```
if (playerPicked[event_id][user_id] >= 5) return CAP_REACHED;
if (coinTaken[event_id][coin_id]) return ALREADY_TAKEN;
coinTaken[event_id][coin_id] = user_id;
playerPicked[event_id][user_id]++;
addMoney(user_id, 500);
```

### **模块 4️⃣ PVE 普通怪（按伤害占比分配）**

#### **功能目标**

后台定时生成怪物，全服玩家可攻击。怪物血量共用，全员共享战斗。

#### **怪物模板**

| **档次** | **HP** | **攻击** | **防御** | **奖励池**  | **参与阈值** |
| -------- | ------ | -------- | -------- | ----------- | ------------ |
| 普通怪   | 6,000  | 6        | 24       | 30,000 金币 | 伤害 ≥ 0.3%  |

#### **战斗机制**

- 自动战斗，每秒一次结算；
- 玩家伤害：max(1, 武力 - 怪物防御)；
- 怪物随机攻击 1~3 名玩家；
- 怪物血量为全局共享；
- 怪物死亡时：按伤害占比分金币奖励；
- 伤害数据缓存在内存中，战斗结束后写入 SQLite。

#### **事件流**

| **事件**       | **内容**                     |
| -------------- | ---------------------------- |
| monster.spawn  | 广播怪物出现、HP、奖励池等   |
| monster.update | 每秒广播当前 HP 与伤害榜前 3 |
| monster.end    | 广播怪物死亡、结算结果       |

#### **奖励计算公式**

```
reward_user = totalReward * (userDamage / totalDamage)
if (userDamage < totalDamage * 0.003) reward_user = 0;
```

### **模块 5️⃣ 切磋（仅战报）**

#### **功能目标**

玩家可点选他人发起切磋，自动文字战斗展示，不会死亡，仅显示胜负。

#### **战斗公式**

- 攻击节奏：每秒 1 次；
- 伤害：max(1, 攻击 - 防御)；
- 双方轮流攻击；
- 战斗结束条件：任意一方 HP ≤ 0；
- 胜者广播私有战报（仅双方可见）。

#### **WebSocket 事件**

| **事件**      | **含义**                   |
| ------------- | -------------------------- |
| pvp.challenge | 发起切磋（目标ID）         |
| pvp.result    | 返回战报与胜负结果         |
| pvp.update    | 战斗进程（回合制文字信息） |

## **四、数据库设计（SQLite）**

| **表名**        | **字段**                                                    | **描述**       |
| --------------- | ----------------------------------------------------------- | -------------- |
| users           | id, username, password_hash, created_at                     | 账号信息       |
| characters      | id, user_id, level, exp, money, atk, def, hp, last_exp_time | 玩家属性       |
| messages        | id, user_id, content, type, ts                              | 聊天与系统消息 |
| monsters        | id, hp, max_hp, atk, def, reward_pool, started_at, ended_at | 当前怪物状态   |
| monster_damage  | monster_id, user_id, damage                                 | 怪物伤害统计   |
| coinrain_events | id, started_at, ended_at                                    | 金币雨活动记录 |
| coinrain_claims | event_id, user_id, coins_collected                          | 玩家拾取记录   |

## **五、运行机制与性能策略**

- **结算循环（1Hz）**：怪物战斗、被动金钱、经验检查。
- **SQLite WAL 模式**：支持多读写；每 0.5 秒批量写入。
- **内存状态缓存**：在线用户、当前怪物、金币雨等事件。
- **掉线恢复**：用户重连可恢复状态。
- **防挂机机制**：存点时间窗口 ±5s；重复点击无效。

## **六、前端界面布局**

| **区域** | **内容**                                     |
| -------- | -------------------------------------------- |
| 左侧     | 玩家信息（等级、经验、HP、金钱、按钮“存点”） |
| 中间     | 聊天与系统事件流（带时间戳）                 |
| 右侧     | 在线玩家列表（点击用户→“切磋”按钮）          |
| 顶部     | 系统事件横幅（怪物出现、金币雨进行中）       |
| 前景层   | 金币雨动画点击区                             |
| 底部     | 聊天输入框（发送公共消息）                   |

## **七、部署建议**

- 部署在单一 Node 进程（或两个进程：Next.js + WS Server）。
- 数据存储在本地 SQLite 文件。
- 定时任务（CRON）用于生成怪物或金币雨。
- 日志写入本地 /logs 文件夹。

## **八、扩展接口（预留）**

| **功能**         | **状态** |
| ---------------- | -------- |
| 杀人香真实PVP    | 暂不实现 |
| 帮派/私聊频道    | 暂不实现 |
| 装备系统         | 暂不实现 |
| 后台面板         | 后续版本 |
| Redis 分布式扩展 | 后续版本 |

## **九、交付验收标准**

✅ 玩家可注册/登录并进入聊天室；

✅ 公共消息实时同步；

✅ 存点与被动金钱自动工作，升级数值正确；

✅ 后台触发金币雨，玩家点击拾取并受上限约束；

✅ 怪物可出现并被群体击杀，按伤害分配金币、经验；

✅ 可发起切磋并显示胜负；

✅ 所有状态实时刷新并持久化；

✅ 100 并发下系统稳定，SQLite 无锁死。

